import React, { Component } from 'react';
import { View, Text } from 'react-native';

export default class EventComponent extends Component {
  render() {
    return (
      <View>
        <Text style={{fontSize:20}}>EventComponent  </Text>
      </View>
    );
  }
}
